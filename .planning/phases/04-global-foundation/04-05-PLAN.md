---
phase: 04-global-foundation
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/detail.js
  - src/styles/detail.css
  - src/data/tr-broadcast-catalog.json
  - src/lib/broadcast.js
  - public/img/networks/showtv.png
  - public/img/networks/atv.png
  - public/img/networks/trt.png
  - public/img/networks/startv.png
  - public/img/networks/kanald.png
  - tests/broadcast.test.js
  - tests/fixtures/tmdb-tv-returning-series.json
  - tests/fixtures/tmdb-tv-ended.json
autonomous: false
requirements: [BCAST-01, BCAST-02, BCAST-03]
estimated_context: ~40%
must_haves:
  truths:
    - "TV detail page with status='Returning Series' shows a 'Yayın Kanalı' / 'Airing on' section above streaming providers"
    - "TV detail page with status='Ended' and next_episode_to_air=null does NOT render the broadcast section"
    - "Broadcast section shows network logo + name + next-episode date (relative + absolute)"
    - "TR networks (Show TV, ATV, TRT, Star TV, Kanal D) render curated local PNG logos via overlay"
    - "Non-TR networks (CBS, BBC, etc.) render TMDB-supplied logos"
    - "Movies (media_type=movie) never trigger broadcast section render"
  artifacts:
    - path: "src/lib/broadcast.js"
      provides: "isActivelyAiring() + TR network overlay lookup + Intl.RelativeTimeFormat wrapper"
      exports: ["isActivelyAiring", "getNetworkLogoPath", "formatNextEpisode"]
    - path: "src/data/tr-broadcast-catalog.json"
      provides: "TMDB network ID → curated metadata (logo path, deep link)"
    - path: "src/features/detail.js"
      provides: "Broadcast section render hook before streaming card"
      contains: "isActivelyAiring"
    - path: "tests/broadcast.test.js"
      provides: "active-airing detection + overlay lookup + relative date format tests"
  key_links:
    - from: "src/features/detail.js"
      to: "src/lib/broadcast.js"
      via: "isActivelyAiring(details) gate"
      pattern: "isActivelyAiring\\(details\\)"
    - from: "src/lib/broadcast.js"
      to: "src/data/tr-broadcast-catalog.json"
      via: "getNetworkLogoPath(networkId) lookup"
      pattern: "TR_BROADCAST_CATALOG"
---

<objective>
Render an "Airing on" broadcast card on TV detail pages for actively-airing series, separate from the existing "Nerede İzlenir" streaming section. Curate TR broadcast network logos (Show TV, ATV, TRT, Star TV, Kanal D) via local overlay; fall through to TMDB logos for other regions.

Purpose: Phase 03.2-r14c removed broadcast networks from the streaming catalog because they air content rather than stream it. Phase 4.5 restores broadcast visibility as a distinct UI affordance, leveraging TMDB `networks` + `next_episode_to_air` which are already fetched.

Output: New `src/lib/broadcast.js` module, TR broadcast catalog JSON, 5 local network logos, detail-page render hook, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-global-foundation/04-CONTEXT.md
@.planning/phases/04-global-foundation/04-RESEARCH.md
@src/features/detail.js
@src/styles/detail.css

# Research-resolved facts:
# - src/features/detail.js:1109-1113 already consumes details.next_episode_to_air.air_date, .name, .season_number, .episode_number — fetch infrastructure exists
# - src/services/streaming-cache.js:101-105 already iterates details.networks
# - Phase 03.2-r14c explicitly excluded broadcast networks from src/data/turkish-platform-catalog.json
# - Detection logic: isActivelyAiring = (status === 'Returning Series' || next_episode_to_air != null)
# - TMDB image base: https://image.tmdb.org/t/p/w92{logo_path} for mobile
# - Round 14 favicon download approach is the pattern for local PNG logos
</context>

<assumptions_locked>
- **A3 (TR network TMDB IDs):** ACCEPTED as starting point but treated as **needs verification**. The catalog JSON will scaffold with the research-suggested IDs (Show TV=1280, ATV=1330, Star TV=1268, Kanal D=1283) AND include a TODO marker on each entry. The catalog lookup uses **TMDB ID as primary key with `byName` fallback** so a wrong ID degrades gracefully to TMDB's own logo. Logos are matched by either path. See 04-RESEARCH.md A3.
- **Visual layout:** Separate card with warm-accent border, positioned **above** the "Nerede İzlenir" streaming card. Layout per research §4.5 recommendation: 48×48 logo (left) + network name + next-episode info (right).
- **Multi-network handling:** If `networks[]` has multiple entries, show the network whose `origin_country` matches the user's country (from `localStorage.lumi_locale.country` if available, else `networks[0]`). Defer multi-network grid display.
- **Timezone:** `next_episode_to_air.air_date` is date-only — render via `Intl.DateTimeFormat` + `Intl.RelativeTimeFormat`; do NOT show fake "00:00" time.
- **Logo source:** Local PNGs for 5 TR networks (Show TV, ATV, TRT, Star TV, Kanal D). For ALL others, use TMDB at `https://image.tmdb.org/t/p/w92{logo_path}`.
</assumptions_locked>

<assumptions_locked>
**Network logo asset acquisition (LOCKED — option a):** Executor downloads logos from each network's official site favicon (Round 14 pattern). Two options were considered:
  (a) Download from each network's official site favicon/press kit (Round 14 pattern) during execution
  (b) User supplies the 5 PNGs upfront

**Locked decision:** (a) — executor downloads via curl from `https://www.showtv.com.tr/favicon.ico` etc. and converts to PNG. If a domain blocks scraping, the executor falls back to TMDB's logo for that network and flags it.
</assumptions_locked>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/broadcast.js + TR catalog + fixtures + tests</name>
  <files>src/lib/broadcast.js, src/data/tr-broadcast-catalog.json, tests/broadcast.test.js, tests/fixtures/tmdb-tv-returning-series.json, tests/fixtures/tmdb-tv-ended.json</files>
  <behavior>
    - isActivelyAiring({status:'Returning Series', next_episode_to_air:null}) → true
    - isActivelyAiring({status:'Ended', next_episode_to_air:null}) → false
    - isActivelyAiring({status:'Ended', next_episode_to_air:{air_date:'2026-06-01'}}) → true (OR logic)
    - isActivelyAiring({status:'Canceled', next_episode_to_air:null}) → false
    - getNetworkLogoPath({id:1280, name:'Show TV', logo_path:'/x.png'}) → '/img/networks/showtv.png' (overlay win)
    - getNetworkLogoPath({id:16, name:'CBS', logo_path:'/y.png'}) → 'https://image.tmdb.org/t/p/w92/y.png' (TMDB passthrough)
    - getNetworkLogoPath({id:9999, name:'Show TV', logo_path:'/z.png'}) → '/img/networks/showtv.png' (byName fallback when ID mismatched)
    - formatNextEpisode({air_date:'2026-05-15', season_number:5, episode_number:12}, 'tr') → object with relative ("3 gün sonra") + absolute ("15 Mayıs") + label ("S5 E12")
  </behavior>
  <action>
    1. Create `src/data/tr-broadcast-catalog.json`:
       ```json
       {
         "_doc": "TR broadcast network overlay. Primary key: TMDB network ID. Fallback: byNormalizedName. Wrong IDs degrade to TMDB default (graceful).",
         "byId": {
           "1280": { "slug": "showtv", "name": "Show TV", "logo": "/img/networks/showtv.png", "url": "https://www.showtv.com.tr", "_todo": "verify ID" },
           "1330": { "slug": "atv", "name": "ATV", "logo": "/img/networks/atv.png", "url": "https://www.atv.com.tr", "_todo": "verify ID" },
           "1268": { "slug": "startv", "name": "Star TV", "logo": "/img/networks/startv.png", "url": "https://www.startv.com.tr", "_todo": "verify ID" },
           "1283": { "slug": "kanald", "name": "Kanal D", "logo": "/img/networks/kanald.png", "url": "https://www.kanald.com.tr", "_todo": "verify ID" }
         },
         "byNormalizedName": {
           "show tv": "1280",
           "atv": "1330",
           "trt 1": "trt", "trt 2": "trt", "trt": "trt",
           "star tv": "1268",
           "kanal d": "1283"
         },
         "specialEntries": {
           "trt": { "slug": "trt", "name": "TRT", "logo": "/img/networks/trt.png", "url": "https://www.trtizle.com" }
         }
       }
       ```
    2. Create `src/lib/broadcast.js` exporting `isActivelyAiring`, `getNetworkLogoPath`, `formatNextEpisode`. Logic:
       - `isActivelyAiring(details)` → `details.status === 'Returning Series' || details.next_episode_to_air != null` (strict equality, no `.toLowerCase()` per research §4.5 pitfall).
       - `getNetworkLogoPath(network)` → check `catalog.byId[network.id]` first, then `catalog.byNormalizedName[network.name.toLowerCase().trim()]`, else `https://image.tmdb.org/t/p/w92${network.logo_path}`.
       - `formatNextEpisode(nextEp, lang)` → returns `{ relative, absolute, label }` using `Intl.RelativeTimeFormat(lang, {numeric:'auto'})` and `Intl.DateTimeFormat(lang, {day:'numeric', month:'long'})`. Label = `S${season_number} E${episode_number}`.
    3. Create fixtures `tests/fixtures/tmdb-tv-returning-series.json` (status: 'Returning Series', next_episode_to_air: {air_date, season_number:5, episode_number:12}, networks:[{id:1280, name:'Show TV', logo_path:'/abc.png'}]) and `tests/fixtures/tmdb-tv-ended.json` (status: 'Ended', next_episode_to_air: null, networks:[]).
    4. Create `tests/broadcast.test.js` covering all 8 behavior cases above. Mock `Date.now()` for stable relative-date assertions.
    5. Run `npm test -- tests/broadcast.test.js --run` → all green.
  </action>
  <verify>
    <automated>cd C:/Users/berka/.gemini/antigravity/projects/lumi && npm test -- tests/broadcast.test.js --run</automated>
  </verify>
  <done>
    - 8+ behavior cases pass
    - Catalog JSON has 5 TR network entries with `_todo` markers for ID verification
    - Module exports 3 functions with stable signatures
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire broadcast section into src/features/detail.js + acquire network logos</name>
  <files>src/features/detail.js, src/styles/detail.css, public/img/networks/showtv.png, public/img/networks/atv.png, public/img/networks/trt.png, public/img/networks/startv.png, public/img/networks/kanald.png</files>
  <action>
    1. Read `src/features/detail.js` around line 1109 (existing `next_episode_to_air` consumption) to identify the streaming card render location.
    2. Acquire 5 PNG logos. Try in order: (a) `curl -L https://www.showtv.com.tr/favicon.ico` etc., convert to PNG, target 96×96 px; (b) if scrape fails, fall back to TMDB's network logo (`https://image.tmdb.org/t/p/w92{network.logo_path}`) downloaded to local. Save to `public/img/networks/{slug}.png`. If a logo cannot be acquired, the catalog's overlay lookup will return the path but the `<img>` will 404 — acceptable; CSS should `onerror` fallback to TMDB URL.
    3. Add broadcast render block in `detail.js`:
       ```js
       import { isActivelyAiring, getNetworkLogoPath, formatNextEpisode } from '../lib/broadcast.js';

       if (details.media_type === 'tv' && isActivelyAiring(details) && details.networks?.length) {
         const userCountry = JSON.parse(localStorage.getItem('lumi_locale') || '{}').country;
         const primary = details.networks.find(n => n.origin_country === userCountry) || details.networks[0];
         const logo = getNetworkLogoPath(primary);
         const nextEp = details.next_episode_to_air
           ? formatNextEpisode(details.next_episode_to_air, window.i18n.currentLang)
           : null;
         // render <section class="detail-airing-info"> ... </section> ABOVE streaming card
       }
       ```
    4. Add CSS for `.detail-airing-info` in `src/styles/detail.css`: warm accent border (e.g., `border-left: 3px solid #f4a261`), 48×48 logo on left, name + next-episode info on right. Match mobile-first design tokens already in the file. Position the section so it renders before the "Nerede İzlenir" card in DOM order.
    5. Add `<img onerror="this.src='https://image.tmdb.org/t/p/w92'+...">` fallback for the logo so a missing local PNG degrades to TMDB. Or simpler: in `getNetworkLogoPath`, if the overlay path doesn't exist on disk, return the TMDB URL — but file-existence checks aren't available client-side, so the `onerror` attribute is the right place.
    6. Section title: use `t('airingOn')` key — add this key to `translations.tr` ("Yayın Kanalı") and `translations.en` ("Airing on") in `public/i18n.js` (additive change; backward compatible with 04-01).
    7. For ended shows (`!isActivelyAiring(details)`): no render, no DOM flash. Render gate must be inside the data-resolved code path (per research pitfall #5).
  </action>
  <verify>
    <automated>cd C:/Users/berka/.gemini/antigravity/projects/lumi && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `detail-airing-info` section renders for TV with `Returning Series` or non-null `next_episode_to_air`
    - Section hidden for movies and ended shows
    - Logo `<img>` has `onerror` fallback to TMDB URL
    - i18n keys `airingOn` added to TR + EN dictionaries
    - All vitest tests still pass (137 baseline + new broadcast tests)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile QA — broadcast card on real TR series</name>
  <what-built>Broadcast info card on TV detail pages for actively-airing series, with TR network overlay.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (or whatever the project uses)
    2. Open Chrome DevTools mobile emulation (iPhone 14 viewport, 390×844)
    3. Navigate to a known TR returning series — e.g., search "Yargı" or "Aşk Mantık İntikam" → open detail
    4. Confirm:
       - "Yayın Kanalı" card renders ABOVE "Nerede İzlenir"
       - Network logo is the local PNG (curated) or TMDB fallback (no broken-image icon)
       - Next-episode line shows relative date ("3 gün sonra") + S/E label if available
    5. Navigate to an ended show — e.g., "Breaking Bad" → open detail
    6. Confirm: no "Yayın Kanalı" section appears anywhere
    7. Navigate to a movie (e.g., "Inception")
    8. Confirm: no broadcast section (movies excluded entirely)
    9. Switch language via devtools `i18n.setLanguage('en')` and reload
    10. Confirm: "Airing on" label renders, relative date in English ("in 3 days")
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., "logo broken for ATV", "section flashes for ended show")</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- tests/broadcast.test.js --run` → green
- Manual QA confirms section visibility per series type
- `grep -n "isActivelyAiring" src/features/detail.js` → returns the import + call site
- No regression on 137-baseline vitest count (6 pre-existing toast-mock failures remain out of scope)
</verification>

<success_criteria>
- TV active series: broadcast card visible, network logo + name + next-episode info correct
- TV ended series: broadcast card absent
- Movies: broadcast card never appears
- TR networks: curated PNG via overlay (or TMDB fallback on logo 404)
- Non-TR networks: TMDB logo URL
- Tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-global-foundation/04-05-SUMMARY.md` documenting:
- Detection logic + verified TMDB field semantics
- TR catalog structure (byId + byNormalizedName + specialEntries)
- Logo acquisition method used (scrape vs TMDB fallback) per network
- Resolved/unresolved assumptions: A3 (TR network IDs need user verification post-deploy)
</output>
