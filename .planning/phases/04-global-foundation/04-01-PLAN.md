---
phase: 04-global-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/i18n.js
  - public/i18n/en.json
  - public/i18n/tr.json
  - public/i18n/es.json
  - public/i18n/fr.json
  - public/i18n/de.json
  - public/i18n/ja.json
  - public/i18n/ko.json
  - api/search.js
  - api/tmdb.js
  - api/gemini.js
  - tests/i18n.test.js
  - tests/i18n-coverage.test.js
  - tests/api-search-lang.test.js
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04]
estimated_context: ~45%
must_haves:
  truths:
    - "Calling t('unknownKey') returns the English value when available, raw key only as last resort"
    - "All TR translation keys also exist in EN (no missing fallbacks)"
    - "ES, FR, DE, JA, KO dictionaries are scaffolded with the EN keyset (empty strings allowed)"
    - "Setting currentLang='en' on first install (no stored override) renders English UI"
    - "api/search.js, api/tmdb.js, api/gemini.js accept a lang param and translate to BCP 47 for TMDB"
    - "Gemini prompts include a 'Respond in {language}.' instruction matched to the request lang"
  artifacts:
    - path: "public/i18n.js"
      provides: "t() with EN fallback chain + 8 scaffolded languages"
      contains: "this.translations[this.currentLang]?.[key] ?? this.translations.en?.[key] ?? key"
    - path: "public/i18n/es.json"
      provides: "ES stub matching EN keyset"
    - path: "public/i18n/fr.json"
      provides: "FR stub matching EN keyset"
    - path: "public/i18n/de.json"
      provides: "DE stub matching EN keyset"
    - path: "public/i18n/ja.json"
      provides: "JA stub matching EN keyset"
    - path: "public/i18n/ko.json"
      provides: "KO stub matching EN keyset"
    - path: "api/search.js"
      provides: "Dynamic TMDB language param"
      contains: "BCP_47"
    - path: "tests/i18n.test.js"
      provides: "Fallback chain unit test"
    - path: "tests/i18n-coverage.test.js"
      provides: "EN ⊇ TR keyset assertion"
  key_links:
    - from: "public/i18n.js#t()"
      to: "this.translations.en"
      via: "nullish coalescing fallback"
      pattern: "translations.en\\?\\.\\[key\\]"
    - from: "api/search.js"
      to: "TMDB /search endpoint"
      via: "BCP 47 language param"
      pattern: "language=.*BCP_47"
---

<objective>
Promote EN to the default fallback language, complete TR/EN coverage, scaffold 5 additional language stubs, and plumb the `lang` parameter end-to-end from frontend to TMDB + Gemini backends.

Purpose: Foundation for global launch (Phase 6 iOS). Without EN-first, missing keys leak camelCase identifiers to non-TR users. Without `lang` plumbing, TMDB/Gemini responses are stuck in Turkish regardless of user preference.

Output: Updated `public/i18n.js` with fallback chain, 5 scaffolded language JSONs, 3 API endpoints accepting `lang`, and unit tests proving fallback + coverage + API param plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-global-foundation/04-CONTEXT.md
@.planning/phases/04-global-foundation/04-RESEARCH.md
@public/i18n.js
@api/search.js

# Research-resolved facts (do NOT re-research):
# - i18n dictionary is INLINE at public/i18n.js lines 5-971 (NOT src/i18n/*.json — those paths are wrong in CONTEXT.md)
# - public/i18n/{en,tr}.json exist as stub files but are NOT loaded by i18n.js (forward-looking stubs)
# - HTML has ZERO data-i18n attributes; updateTranslations() is a no-op against current markup
# - Only src/features/detail.js (19x) and src/features/profile.js (5x) use t() / data-i18n
# - api/search.js:56 hardcodes language: 'tr-TR' → must become dynamic
# - TMDB requires BCP 47 (xx-XX), not bare ISO-639 (xx)
</context>

<assumptions_locked>
- **A1 (no caller depends on raw-key TR leak):** ACCEPTED. Fallback chain is strictly better; if any caller deliberately relied on raw-key returns we'll surface the issue via the coverage test. See 04-RESEARCH.md A1.
- **Dictionary format:** Keep inline JS object in `public/i18n.js` for this phase (smaller diff, less risk per research §4.1). Stub JSON files in `public/i18n/{lang}.json` get the EN keyset but are NOT lazy-loaded yet. Future phase may migrate to JSON modules.
- **Audit scope:** This plan ONLY fixes (a) fallback chain, (b) ES/FR/DE/JA/KO scaffolds, (c) backend `lang` plumbing. **Full hardcoded-string-to-`t()` audit is deferred** — too large for this plan (HTML has 0 data-i18n attrs, most src/ modules render raw template literals). A follow-up `04-01b-PLAN.md` may be created if audit volume warrants; for now we ensure the **mechanism** is correct so the audit can run in isolation later.
</assumptions_locked>

<tasks>

<task type="auto">
  <name>Task 1: Refactor t() fallback chain + scaffold 5 stub language dictionaries</name>
  <files>public/i18n.js, public/i18n/es.json, public/i18n/fr.json, public/i18n/de.json, public/i18n/ja.json, public/i18n/ko.json</files>
  <behavior>
    - t('exists_in_tr_only') with currentLang='tr' → returns TR value
    - t('exists_in_en_only') with currentLang='tr' → returns EN value (fallback)
    - t('exists_nowhere') with any lang → returns raw key
    - t() with currentLang='es' on a key that exists in EN but not ES → returns EN value
    - setLanguage('en-US') normalizes to 'en' (existing behavior preserved)
  </behavior>
  <action>
    1. Open `public/i18n.js` and locate the `t(key)` method (~line 974).
    2. Replace its body with:
       ```js
       t(key) {
         return this.translations[this.currentLang]?.[key]
             ?? this.translations.en?.[key]
             ?? key;
       }
       ```
    3. Audit TR keyset vs EN keyset in the inline dictionary. For every key present in `translations.tr` but missing in `translations.en`, add an English translation. Use existing UI copy (CONTEXT.md screenshots / inline JS template literals) as source. Do not delete any TR keys.
    4. Extract the EN keyset (list of all top-level keys in `translations.en`). For each of `es`, `fr`, `de`, `ja`, `ko`, write `public/i18n/{lang}.json` as a JSON object with that exact keyset and **empty-string values** (translators fill later). The `t()` fallback chain ensures empty strings degrade to EN.
    5. ALSO add empty-stub `translations.es/fr/de/ja/ko = {}` entries to the inline dictionary in `public/i18n.js` if not already present — so `setLanguage('es')` doesn't crash. The objects can be `{}` since EN fallback handles all lookups.
    6. Keep `currentLang` initialization behavior: if `localStorage.appLanguage` is unset, default should be `'en'` (NOT `'tr'`). Change the init default at line ~982 if it currently reads `'tr'`.
    7. Do NOT modify any HTML or call sites — fallback chain is transparent to callers.
  </action>
  <verify>
    <automated>cd C:/Users/berka/.gemini/antigravity/projects/lumi && npm test -- tests/i18n.test.js tests/i18n-coverage.test.js --run</automated>
  </verify>
  <done>
    - `t()` fallback chain test (Task 3) passes
    - EN keyset ⊇ TR keyset (coverage test passes)
    - 5 new stub JSONs exist with matching EN keyset
    - Default lang is `'en'` when localStorage empty
  </done>
</task>

<task type="auto">
  <name>Task 2: Plumb lang param through api/search.js, api/tmdb.js, api/gemini.js</name>
  <files>api/search.js, api/tmdb.js, api/gemini.js, tests/api-search-lang.test.js</files>
  <behavior>
    - POST /api/search with body `{ q: 'inception', lang: 'en' }` → TMDB called with `language=en-US`
    - POST /api/search with body `{ q: 'inception', lang: 'tr' }` → TMDB called with `language=tr-TR`
    - POST /api/search with body `{ q: 'inception' }` (no lang) → defaults to `language=en-US` (NOT tr-TR)
    - POST /api/search with body `{ q: 'inception', lang: 'invalid' }` → falls back to 'en-US' (allowlist enforced)
    - POST /api/gemini receives lang and includes "Respond in {languageName}." prefix in prompt
  </behavior>
  <action>
    1. Define a shared BCP 47 mapping at the top of `api/search.js` (and re-export or duplicate to other API files — Edge functions are file-scoped, no shared util pattern established yet):
       ```js
       const BCP_47 = { tr: 'tr-TR', en: 'en-US', de: 'de-DE', fr: 'fr-FR', es: 'es-ES', ja: 'ja-JP', ko: 'ko-KR', zh: 'zh-CN' };
       const LANG_NAME = { tr: 'Türkçe', en: 'English', de: 'Deutsch', fr: 'Français', es: 'Español', ja: '日本語', ko: '한국어', zh: '中文' };
       function resolveLang(raw) { return BCP_47[raw] ? raw : 'en'; }
       ```
    2. `api/search.js`: read `lang` from request body (POST) or query string (GET). Replace the hardcoded `language: 'tr-TR'` at line 56 with `language: BCP_47[resolveLang(lang)]`. Maintain `export const config = { runtime: 'edge' }` (CRITICAL per Phase 03.2-r9 lesson).
    3. `api/tmdb.js`: same pattern — accept `lang` query param, pass through as `language=BCP_47[lang]` to TMDB. Preserve `runtime: 'edge'` config block.
    4. `api/gemini.js`: accept `lang`. Prepend `Respond in ${LANG_NAME[resolveLang(lang)]}.\n\n` to the existing prompt. Preserve `runtime: 'edge'` config block.
    5. **DO NOT** use `export const runtime = 'edge'` syntax — it's silently ignored. ONLY use `export const config = { runtime: 'edge' }`.
    6. Update frontend callers (`src/features/search.js`, `src/features/detail.js` if they call these endpoints) to pass `lang: window.i18n.currentLang` in request bodies. Search the call sites with Grep for `/api/search`, `/api/tmdb`, `/api/gemini`.
  </action>
  <verify>
    <automated>cd C:/Users/berka/.gemini/antigravity/projects/lumi && npm test -- tests/api-search-lang.test.js --run</automated>
  </verify>
  <done>
    - All 3 API endpoints honor `lang` param
    - Default = 'en' (NOT 'tr')
    - Invalid lang falls back to 'en'
    - All 3 endpoints retain `export const config = { runtime: 'edge' }`
    - Gemini prompt includes language instruction
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write i18n + API lang tests</name>
  <files>tests/i18n.test.js, tests/i18n-coverage.test.js, tests/api-search-lang.test.js</files>
  <behavior>
    - tests/i18n.test.js: fallback chain (5 cases — TR hit, EN fallback, raw key, ES → EN, setLanguage normalization)
    - tests/i18n-coverage.test.js: every TR key has an EN counterpart (loop assertion); list missing keys in failure message
    - tests/api-search-lang.test.js: resolveLang allowlist (4 cases — valid 'en', valid 'tr', invalid 'xx' → 'en', undefined → 'en'); BCP_47 mapping (5 cases)
  </behavior>
  <action>
    1. Create `tests/i18n.test.js` importing the `i18n` object. Use Vitest. Mock localStorage. Test the 5 fallback-chain cases above. Reference public/i18n.js directly via dynamic import or by loading the file content into a sandbox (look at existing tests for the pattern — `tests/favorites-storage.test.js` likely shows the convention).
    2. Create `tests/i18n-coverage.test.js`: loads `i18n.translations.tr` and `i18n.translations.en`, iterates TR keys, asserts each exists in EN. Failure message lists ALL missing keys (not just first).
    3. Create `tests/api-search-lang.test.js`: import `resolveLang` and `BCP_47` from `api/search.js` (export them if not exported). Assert mapping + fallback behavior. Do NOT make real HTTP calls — pure unit test.
    4. Run `npm test` → all 3 new test files green, no regression on 137-test baseline (acknowledge 6 pre-existing `tests/detail.test.js` toast-mock failures are OUT OF SCOPE).
  </action>
  <verify>
    <automated>cd C:/Users/berka/.gemini/antigravity/projects/lumi && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 3 new test files pass
    - Total Vitest count: 137 baseline + new tests; the 6 pre-existing detail.test.js failures unchanged (not fixed, not made worse)
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/i18n.test.js tests/i18n-coverage.test.js tests/api-search-lang.test.js --run` → all green
- Manual: open app with `localStorage.clear()` → English UI renders (default = en)
- Manual: switch to `setLanguage('es')` from devtools → renders EN strings (empty ES stubs fall through to EN, no raw keys leak)
- `grep "export const runtime = 'edge'" api/*.js` → returns nothing (only `config = { runtime: 'edge' }` allowed)
</verification>

<success_criteria>
- EN is the default language for new installs
- `t()` never leaks raw keys to UI for any key that exists in EN
- 5 new stub JSONs scaffold the keyset for future translators
- All 3 API endpoints accept + honor `lang` param
- All edge functions retain `config = { runtime: 'edge' }` syntax
- 3 new test files green
</success_criteria>

<output>
After completion, create `.planning/phases/04-global-foundation/04-01-SUMMARY.md` documenting:
- Fallback chain mechanism + before/after behavior
- New localStorage key behavior (`appLanguage` default = 'en')
- BCP 47 mapping table for backend
- Audit deferral note: hardcoded-string-to-`t()` audit deferred to follow-up; mechanism ready
- Resolved assumption: A1 (verified by coverage test)
</output>
