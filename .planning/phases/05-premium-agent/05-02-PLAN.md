---
phase: 05-premium-agent
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/ui/paywall-sheet.js
  - src/styles/paywall.css
  - src/features/onboarding.js
  - src/features/discover.js
  - src/lib/entitlements.js
  - src/lib/usage.js
  - public/i18n/*.json
  - .planning/decisions/PREMIUM-PRICING.md
  - tests/paywall-sheet.test.js
  - tests/paywall-trigger.test.js
autonomous: false
must_haves:
  truths:
    - "A single shared paywall sheet component renders the onboarding premium content (4 features + 3 tiers + 3-day trial)"
    - "Onboarding premium slide and in-app paywall use the SAME component — no duplicated markup"
    - "Paywall opens on (1) the 6th AI query (429 from 05-01) and (2) tapping any premium-gated action"
    - "CTA is env-split: prod shows 'iOS/Android app'imizde Premium' store CTA; dev shows mock-unlock"
    - "Dev mock-unlock flips Firestore premium via setPremiumMock(true) and closes the sheet unlocked"
    - "Discover shows remaining free quota ('Bugün 2/5') sourced from usage.js"
    - "PREMIUM-PRICING.md trial corrected 7→3 days in §1, §8, §12 with a dated correction note"
  outputs:
    - "src/ui/paywall-sheet.js — openPaywall({trigger}), shared by onboarding + in-app"
    - "src/styles/paywall.css — extracted from onboarding premium slide styles"
    - "Quota indicator in discover header"
    - "i18n keys for paywall + store-CTA + quota across all locales"
    - "Corrected PREMIUM-PRICING.md"
---

<objective>
Turn the onboarding mock paywall into a real, reusable conversion surface and wire its triggers.

The onboarding premium slide (onboarding.js buildPremium) already renders 4 features + 3 tiers.
Extract that into `src/ui/paywall-sheet.js` (a bottom sheet) so BOTH onboarding and the in-app
paywall render identical content from one source. Wire two triggers from 05-01's gate:
the 6th-query 429 and any premium-feature tap. Split the CTA by environment: production = App
Store/Play store CTA (web sells nothing, per PREMIUM-PRICING.md §4); dev = mock-unlock that flips
the Firestore premium flag so we can test the four features. Also correct the trial-length conflict.

TDD: `tests/paywall-trigger.test.js` (429 → openPaywall, feature tap → openPaywall, env-split CTA
selection) and `tests/paywall-sheet.test.js` (renders 4 features/3 tiers, trial=3, mock-unlock path) first.
</objective>

<context>
@.planning/phases/05-premium-agent/05-CONTEXT.md
@src/features/onboarding.js
@src/lib/entitlements.js
@src/lib/usage.js
</context>

<tasks>
- [ ] **T1 — Correct trial length**: PREMIUM-PRICING.md §1/§8/§12 "7 day" → "3 day" + dated correction note.
- [ ] **T2 — Extract shared sheet (TDD)**: `paywall-sheet.js` + `paywall.css` from onboarding buildPremium;
  refactor onboarding to consume it (keep 44 onboarding tests green). `openPaywall({trigger})`.
- [ ] **T3 — Env-split CTA (TDD)**: prod = store links (App Store + Play); dev (`import.meta.env.DEV`) =
  mock-unlock button → `setPremiumMock(true)`. Store URLs as config placeholders (filled Phase 6).
- [ ] **T4 — Triggers (TDD)**: discover/AI flow catches 429 from 05-01 → openPaywall({trigger:'quota'});
  premium-gated taps → openPaywall({trigger:'feature'}). Premium users never see it.
- [ ] **T5 — Quota indicator**: discover header shows remaining free queries from usage.js.
- [ ] **T6 — Verify**: `npm test` green; manual: 6th query opens sheet, dev unlock works, prod build shows store CTA.
</tasks>

<verification>
- `npm test` green (onboarding suite intact + new paywall suites)
- Manual: quota indicator counts down; 6th query → sheet; dev unlock → features accessible; `vite build` prod → store CTA only
</verification>
