---
phase: 05-premium-agent
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/features/agent-hub.js
  - src/features/decide-for-me.js
  - src/lib/seen.js
  - src/styles/agent.css
  - index.html
  - src/main.js
  - api/gemini.js
  - public/i18n/*.json
  - tests/seen.test.js
  - tests/decide-for-me.test.js
autonomous: false
must_haves:
  truths:
    - "A dedicated Agent hub surfaces the Movie Night Agent identity (entry from Home / ✨ surface, not a 5th nav tab)"
    - "seen.js derives the 'seen' set = userRatings ∪ liked_items; watchlist_items is a positive signal"
    - "Decide-for-Me is single-tap: time-of-day auto-inferred from device clock; mood chips optional"
    - "Decide-for-Me returns EXACTLY 1 unseen title honoring owned platforms + mood + time, via api/gemini.js feature=decide"
    - "Result shows as a dramatic reveal card with a 'başka öner' (re-roll) option"
    - "Free users tapping Decide-for-Me get the paywall (trigger:'feature'); premium runs uncapped"
  outputs:
    - "src/features/agent-hub.js — the Agent center shell + premium gating"
    - "src/features/decide-for-me.js — input (mood chips + inferred time) → 1-title reveal"
    - "src/lib/seen.js — getSeenSet(), getTasteProfile() reused by Pair Mode (05-04)"
    - "api/gemini.js feature=decide branch (grounded prompt, excludes seen set, filters to owned platforms)"
    - "Agent styles + i18n"
---

<objective>
Build the Agent hub and the first proactive feature, Decide-for-Me.

The Agent hub is the home of Lumi's "Movie Night Agent" identity — a single surface housing
Decide-for-Me now, Pair Mode (05-04) and Notifications (05-05) next. Decide-for-Me is the
flagship: one tap, AI picks exactly one title you haven't seen, tuned to your owned platforms,
an optional mood chip, and the inferred time-of-day. The pick lands as a dramatic reveal card
with a re-roll. `src/lib/seen.js` centralizes the derived "seen" set (ratings ∪ likes) and a
taste profile — both reused by Pair Mode.

TDD: `tests/seen.test.js` (derivation from ratings/likes, watchlist excluded from "seen") and
`tests/decide-for-me.test.js` (exactly-1 result, excludes seen, respects platforms, time inference,
free→paywall / premium→run) first.
</objective>

<context>
@.planning/phases/05-premium-agent/05-CONTEXT.md
@src/features/favorites-storage.js
@src/features/profile.js
@src/lib/entitlements.js
@api/gemini.js
@index.html
</context>

<tasks>
- [ ] **T1 — seen.js (TDD)**: `getSeenSet()` = ids from userRatings ∪ liked_items; `getTasteProfile()`
  (genres/likes summary for prompts). watchlist = signal, not "seen".
- [ ] **T2 — Agent hub shell**: `agent-hub.js` + entry from Home (✨), premium-aware; free users see
  locked feature cards that open the paywall. Hub renders Decide-for-Me entry now; slots for 05-04/05-05.
- [ ] **T3 — Decide-for-Me (TDD)**: mood chips (optional) + auto time-of-day; call api/gemini.js
  feature=decide with seen-exclusion + owned-platform filter; reveal card + re-roll.
- [ ] **T4 — gemini.js feature=decide**: grounded single-pick prompt, returns 1 title+year+reason; resolve via TMDB.
- [ ] **T5 — Verify**: `npm test` green; manual: hub opens, Decide-for-Me returns 1 unseen on-platform title, re-roll differs, free→paywall.
</tasks>

<verification>
- `npm test` green (new seen + decide suites)
- Manual: Agent hub reachable; Decide-for-Me single-tap reveal; never returns a seen title; free user gated
</verification>
