---
phase: 03.1-mobile-qa-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/detail.js
  - src/features/search.js
  - src/features/discover.js
  - src/main.js
  - index.html
  - index_lumi.css
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]

must_haves:
  truths:
    - "User can scroll the page after closing a detail modal"
    - "User sees autocomplete dropdown suggestions while typing in the search bar"
    - "User can tap mood/era chips on Discover screen and get relevant results"
    - "Discover results title is fully visible below the header, not hidden behind it"
  artifacts:
    - path: "src/features/detail.js"
      provides: "Scroll lock cleanup on all modal close paths"
      contains: "document.body.style.overflow = ''"
    - path: "src/main.js"
      provides: "No autocomplete name collision"
    - path: "index.html"
      provides: "Discover button routes to wizard search when chips selected"
  key_links:
    - from: "detail.js closeModal()"
      to: "document.body.style.overflow"
      via: "direct style reset"
      pattern: "overflow.*=.*''"
    - from: "index.html search input"
      to: "inline showAutocomplete()"
      via: "input event handler"
      pattern: "showAutocomplete"
    - from: "index.html discover button"
      to: "handleWizardSearch or handleAISearch"
      via: "onclick conditional"
      pattern: "handleWizardSearch|handleAISearch"
---

<objective>
Fix the three critical app-breaking bugs: scroll lock after modal close, missing autocomplete dropdown, and broken discover screen.

Purpose: These bugs make the app unusable — users cannot scroll after viewing a movie, cannot find content via search suggestions, and cannot use mood/era discovery chips. All three must be fixed before any other work.
Output: Three critical interaction flows restored to working state on mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-mobile-qa-fixes/03.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scroll lock after modal close (BUG-01)</name>
  <files>src/features/detail.js</files>
  <read_first>src/features/detail.js, index.html</read_first>
  <action>
Fix the `closeModal()` function in `src/features/detail.js` to reliably clear ALL scroll lock mechanisms on every close path.

1. Find the `closeModal()` function (around line 158-180). Add these defensive resets BEFORE any early return:
   ```javascript
   document.body.style.overflow = '';
   document.body.style.position = '';
   document.documentElement.style.overflow = '';
   document.body.classList.remove('modal-open');
   ```

2. Also find the `openDetail()` function (around line 26). Verify it sets `document.body.style.overflow = 'hidden'` and `document.body.classList.add('modal-open')`. These are the two mechanisms that must be reversed.

3. Search `index.html` for ALL modal close handlers — look for `.modal-backdrop` click handler and `.modal-close` button click handler (around lines 1500-1520). Ensure these handlers ALSO call `document.body.style.overflow = ''` and `document.body.classList.remove('modal-open')`. If they call `closeModal()` from the module, that is sufficient. If they have their own inline close logic, add the overflow reset there too.

4. Search for any CSS rule `.modal-open` in `src/styles/modals.css` or `index_lumi.css` that sets `overflow: hidden` — if found, the `classList.remove('modal-open')` is the critical fix path.

5. Also restore bottom nav visibility in closeModal: `document.querySelector('.bottom-nav').style.display = '';`

The key insight: there are TWO close code paths (detail.js `closeModal()` AND inline script handlers). BOTH must clear overflow. The safest fix is to ensure `closeModal()` is called from all paths and it handles all cleanup.
  </action>
  <verify>
    <automated>npx vitest run tests/detail.test.js -t "closeModal" 2>/dev/null; grep -n "overflow.*=.*''" src/features/detail.js | head -5</automated>
  </verify>
  <done>After opening and closing a detail modal, `document.body.style.overflow` is empty string, `document.body.classList` does not contain 'modal-open', and the page scrolls normally.</done>
  <acceptance_criteria>
    - src/features/detail.js closeModal() contains `document.body.style.overflow = ''`
    - src/features/detail.js closeModal() contains `document.body.classList.remove('modal-open')`
    - src/features/detail.js closeModal() contains `document.documentElement.style.overflow = ''`
    - src/features/detail.js closeModal() contains bottom nav display restore
    - All modal close paths in index.html route through closeModal() OR independently clear overflow
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restore autocomplete dropdown (BUG-02)</name>
  <files>src/main.js, src/features/search.js, index.html</files>
  <read_first>src/main.js, src/features/search.js, index.html</read_first>
  <action>
Fix the autocomplete name collision where `src/main.js` overwrites the inline `showAutocomplete(query)` with the module version that expects a results array.

1. Open `src/main.js` and find the line `window.showAutocomplete = showAutocomplete;` (around line 294). Either:
   - **Option A (preferred):** Remove this line entirely so the inline script's `showAutocomplete(query)` survives. The module version should be accessed via `window.LumiModules.handleAutocomplete` if needed.
   - **Option B:** Rename to `window.showAutocompleteSuggestions = showAutocomplete;` to avoid collision.

2. Verify the inline script in `index.html` has a working `showAutocomplete(query)` function (around lines 1875-1958) that:
   - Takes a query string
   - Fetches TMDB multi-search results
   - Renders an autocomplete dropdown with clickable items
   - Each item opens the detail modal on click

3. Verify the search input handler in `index.html` (around line 1369) calls `showAutocomplete(query)` on input events when query length >= 2.

4. Check if `handleAutocomplete()` in `src/features/search.js` is a better, more complete version. If so, update the inline input handler to call `window.LumiModules.handleAutocomplete()` instead, and remove the inline `showAutocomplete` function. But ONLY if the module version handles the full flow (fetch + render + click handlers).

5. Test the fix by verifying no naming collision: after module load, the function that handles search input typing must render visible dropdown items.
  </action>
  <verify>
    <automated>grep -n "window.showAutocomplete" src/main.js; grep -c "showAutocomplete" index.html</automated>
  </verify>
  <done>Typing 2+ characters in the search bar renders an autocomplete dropdown with movie/TV suggestions. Clicking a suggestion opens the detail modal.</done>
  <acceptance_criteria>
    - src/main.js does NOT contain `window.showAutocomplete = showAutocomplete` (or uses a different name)
    - Either inline showAutocomplete(query) exists and fetches TMDB results, OR inline handler calls window.LumiModules.handleAutocomplete()
    - No duplicate function name conflict between inline script and ES module for autocomplete
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix discover screen — mood chips + results title (BUG-03)</name>
  <files>index.html, src/features/discover.js, index_lumi.css</files>
  <read_first>index.html, src/features/discover.js, index_lumi.css</read_first>
  <action>
Fix two issues on the Discover screen: (A) mood/era chip selections are ignored because the button calls `handleAISearch()` instead of `handleWizardSearch()`, and (B) results title renders behind the fixed header.

**Part A — Mood chips functional:**

1. Find the primary action button in the discover/wizard section of `index.html` (around line 482). It currently has `onclick="handleAISearch()"`.

2. Change the onclick to smart routing:
   ```javascript
   onclick="document.getElementById('ai-movie-input').value.trim() ? handleAISearch() : handleWizardSearch()"
   ```
   This means: if the user typed a text query, use AI search. If they only selected mood/era chips (no text), use wizard search which reads the active chips.

3. Also check if ALL other suggestion/mood buttons (the ones user reports as non-functional "except Surpriz Yap") have proper onclick handlers. Find each button in the discover section and verify they either: call `handleWizardSearch()`, call `handleAISearch()` with pre-filled text, or call their specific handler. The "Surpriz Yap" button works — compare its onclick pattern to the broken ones.

4. Verify `handleWizardSearch()` in `src/features/discover.js` reads active mood/era chip selections and passes them to `showDiscoverResults()`. Ensure this function is exported and available on `window` or `window.LumiModules`.

**Part B — Results title under header:**

5. In `index_lumi.css`, find `.discover-results` (around line 1440). The `padding-top` uses `calc(var(--safe-top) + var(--space-lg))` which doesn't account for the fixed header height (~60px).

6. Change to: `padding-top: calc(var(--safe-top, 0px) + 80px)` to ensure the results title clears the header (60px header + 20px breathing room). Provide a fallback value for `--safe-top` in case it's undefined.

7. Also check `#wizard-results-title` and `.discover-results-header` for any `margin-top` or `padding-top` that might need adjusting.
  </action>
  <verify>
    <automated>grep -n "handleWizardSearch\|handleAISearch" index.html | head -10; grep -n "discover-results" index_lumi.css | head -5</automated>
  </verify>
  <done>Selecting mood/era chips and tapping the primary button triggers wizard search with chip-based results. The results title is fully visible below the header, not hidden behind it.</done>
  <acceptance_criteria>
    - index.html primary discover button onclick contains conditional logic checking ai-movie-input value
    - index.html primary discover button calls handleWizardSearch() when no text input present
    - handleWizardSearch is available on window or window.LumiModules
    - index_lumi.css .discover-results padding-top accounts for at least 60px header height
    - #wizard-results-title is visible below the fixed header (padding >= 70px total)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all three tasks:
1. Open a detail modal, close it with X button — page scrolls normally
2. Open a detail modal, close it by tapping backdrop — page scrolls normally
3. Type "inception" in search bar — autocomplete dropdown appears with results
4. Click an autocomplete item — detail modal opens
5. On Discover, select mood chips, tap primary button — results appear
6. Results title is fully visible, not behind header
</verification>

<success_criteria>
- Scroll lock is reliably cleared on ALL modal close paths
- Autocomplete dropdown renders suggestions on typing 2+ characters
- Discover mood/era chips produce search results
- Discover results title visible below fixed header
- No JavaScript console errors on any of these flows
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-mobile-qa-fixes/03.1-01-SUMMARY.md`
</output>
