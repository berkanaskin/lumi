---
phase: 03.2-polish-platform-gaps
plan: 05
type: execute
wave: 3
depends_on: [03.2-02]
files_modified:
  - src/features/discover.js
  - src/styles/discover.css
  - index.html
autonomous: false
requirements: [TRACK-C]
must_haves:
  truths:
    - "Discover/Search screen feels premium — chips are visually upgraded with clear pressed/active states"
    - "Daily recommendation hero remains visible and unchanged in function (locked decision)"
    - "AI textarea has exactly ONE primary action button (no duplicate handlers)"
    - "Tapping the primary button: empty input → wizard search; non-empty input → AI search"
    - "AI search returns results within 5 seconds OR shows clear error state (no silent failures)"
    - "No desktop-only layout patterns introduced (mobile-only)"
  artifacts:
    - path: "src/features/discover.js"
      provides: "Single primary action handler; removed duplicate console-ai-btn"
    - path: "src/styles/discover.css"
      provides: "Restyled mood/era chips, refined console panel spacing, premium visual hierarchy"
  key_links:
    - from: "index.html primary console button"
      to: "discover.js handleConsoleSubmit"
      via: "single click handler that branches on input value"
      pattern: "handleConsoleSubmit"
    - from: "discover.js handleAISearch"
      to: "/api/search"
      via: "SearchService.hybridSearch with explicit error toast"
      pattern: "hybridSearch"
---

<objective>
Restyle the Discover/Search screen for a premium feel and consolidate the AI search button so it actually works.

Purpose: Track C locked decisions. Daily recommendation must stay; AI search "still not working properly"; chips feel generic. RESEARCH.md identifies a duplicate AI button as a likely confusion source.
Output: Restyled chips/console panel + button consolidation + explicit AI error states. Use Stitch MCP if available; otherwise manual CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@src/features/discover.js
@src/styles/discover.css
@index.html

<interfaces>
Existing (keep):
- `#view-wizard` section with `.discover-hero-bg`, `.discover-top-info` (daily pick) — UNCHANGED
- SearchService.hybridSearch(query, opts) → results
- handleAISearch(query), handleWizardSearch(filters)

Consolidation target:
- Single primary button `#console-primary-btn` with handler `handleConsoleSubmit()` that branches on textarea value
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Stitch MCP availability for design generation</decision>
  <context>CONTEXT.md says "Use Stitch MCP if available". RESEARCH.md notes availability is unknown at plan time. Outcome dictates whether Task 2 uses generated design tokens or manual CSS.</context>
  <options>
    <option id="stitch-available">
      <name>Stitch MCP tools (mcp__stitch__*) ARE available</name>
      <pros>Design-system aligned chips/panel; faster visual iteration</pros>
      <cons>One extra round-trip per design</cons>
    </option>
    <option id="stitch-unavailable">
      <name>No Stitch MCP — manual CSS redesign</name>
      <pros>No external dependency; existing design tokens reused</pros>
      <cons>Designer eye required; iterate via screenshots</cons>
    </option>
  </options>
  <resume-signal>Reply "stitch-available" or "stitch-unavailable"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Consolidate AI button + add explicit error states</name>
  <files>src/features/discover.js, index.html</files>
  <action>
    1. In `index.html`, remove the secondary `.console-ai-btn` element. Keep ONE `.console-btn.primary` with id `console-primary-btn`.
    2. In `discover.js`:
       - Add `handleConsoleSubmit()` that reads textarea value: empty → handleWizardSearch(activeFilters); non-empty → handleAISearch(value).
       - Bind `#console-primary-btn` to `handleConsoleSubmit`.
       - Remove the 3-second setTimeout retry in `handleAISearch` (RESEARCH.md flagged it as confusing). Replace with a single try/catch that on failure shows a toast "Arama başarısız oldu. Tekrar deneyin." and re-enables the button.
       - Add a loading state on the button (disabled + spinner class) for the duration of the call.
    3. Verify `/api/search` is reachable; if it 404s the request, surface that in the toast message ("Servis kullanılamıyor"). Do not silently fall back.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Single button; AI failures are user-visible.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle chips + console panel (mobile only)</name>
  <files>src/styles/discover.css</files>
  <action>
    Apply premium chip + panel styling. If decision was `stitch-available`, generate design via Stitch MCP first then translate to CSS using the project's design tokens. Otherwise:
    - Mood/era chips: rounded-full, 36px touch height, gradient border on active state, subtle inner shadow on press, transition 150ms ease.
    - Console panel: tighten padding (px clamp), refine glass background to align with detail page glass tokens, increase contrast between textarea and chip rows.
    - Daily-pick hero: untouched (locked).
    - Results grid: ensure spacing reads as a clear hierarchy below console.
    - DO NOT introduce desktop breakpoints, sidebars, or hover-only affordances. All states must work on touch.
    - Honor `prefers-reduced-motion`.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Visual upgrade complete; no desktop layout introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Discover screen visual refresh + single AI button with reliable error UX.</what-built>
  <how-to-verify>
    1. Open Discover → daily recommendation hero displays unchanged.
    2. Type "cozy 90s rom-coms" → tap primary button → results appear within ~5s OR clear error toast appears (no silent hang).
    3. Clear textarea → tap primary button → wizard/filter results appear (no AI call).
    4. Tap mood chip "Komik" + era chip "2010s" → active states clearly visible.
    5. Toggle reduce-motion → chip transitions stop, content still legible.
    6. Mobile-only check: 375px viewport → no horizontal scroll, no desktop sidebar artifacts.
    7. Force network failure (DevTools offline) → AI search → red toast with retry-able message.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Tests still green
- Manual checks pass on mobile viewport
- Daily-pick hero functionally unchanged
</verification>

<success_criteria>
- Exactly one primary button on console
- AI failures are visible to user
- Chips feel premium per locked decision
- Mobile-only (no desktop additions)
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-05-SUMMARY.md` after completion.
</output>
