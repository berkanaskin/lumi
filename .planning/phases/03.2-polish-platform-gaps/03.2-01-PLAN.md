---
phase: 03.2-polish-platform-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/features/detail.js
  - tests/favorites-storage.test.js
autonomous: true
requirements: [TRACK-D]
must_haves:
  truths:
    - "User taps like on a detail page → item appears in Beğendiklerim list"
    - "User taps watchlist on a detail page → item appears in İzlediklerim list"
    - "Legacy items previously stored in 'favorites'/'watchlist' keys are migrated automatically (no data loss)"
    - "Empty state shows correctly when both lists are empty"
  artifacts:
    - path: "index.html"
      provides: "Fixed loadFavoritesList() reading correct keys + legacy migration"
      contains: "liked_items"
    - path: "tests/favorites-storage.test.js"
      provides: "Unit tests for the localStorage key contract + migration"
  key_links:
    - from: "src/features/detail.js toggleLike/toggleWatchlist"
      to: "index.html loadFavoritesList"
      via: "localStorage keys liked_items / watchlist_items"
      pattern: "liked_items|watchlist_items"
---

<objective>
Fix the Favorites/Watchlist localStorage key mismatch so Beğendiklerim and İzlediklerim show items the user has liked/added.

Purpose: Direct code inspection (RESEARCH.md) confirmed writers use `liked_items` / `watchlist_items` while the readers in index.html use `favorites` / `watchlist` — both lists appear empty. Highest-impact, lowest-risk fix in the phase.
Output: Working favorites/watchlist screens with one-time legacy migration and item shape correction (`poster_path`, `media_type`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@src/features/detail.js
@index.html

<interfaces>
Writer side (src/features/detail.js — already correct):
- toggleLike(item) → localStorage["liked_items"] = JSON.stringify([{ id, media_type, poster_path, title, added_at }, ...])
- toggleWatchlist(item) → localStorage["watchlist_items"] = same shape

Reader side (index.html loadFavoritesList — BROKEN):
- Currently reads localStorage["favorites"] / localStorage["watchlist"]
- Currently reads item.poster (should be item.poster_path) and item.type (should be item.media_type)

Legacy keys (one-time migration target):
- "favorites"  → migrate into "liked_items"
- "watchlist"  → migrate into "watchlist_items"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 unit test for favorites/watchlist storage contract</name>
  <files>tests/favorites-storage.test.js</files>
  <behavior>
    - Test 1: After toggleLike writes to "liked_items", a reader using key "liked_items" returns the item with poster_path and media_type fields.
    - Test 2: Items present in legacy "favorites" key are merged into "liked_items" on first read; legacy key is removed; duplicates by id are de-duplicated.
    - Test 3: Same migration behavior for "watchlist" → "watchlist_items".
    - Test 4: Empty state — both keys empty returns [].
  </behavior>
  <action>
    Create `tests/favorites-storage.test.js` (Vitest) that imports a small pure helper `loadFavoritesItems(listType)` extracted into `src/features/favorites-storage.js` (new file). The helper accepts "liked" | "watched" and performs the migration + read described in RESEARCH.md "Favorites localStorage Fix" code snippet. Use jsdom's localStorage. Mirror the conventions of existing tests under `tests/` (vitest, describe/it, beforeEach to clear localStorage).
  </action>
  <verify>
    <automated>npx vitest run tests/favorites-storage.test.js</automated>
  </verify>
  <done>4 test cases defined; tests run (will RED until Task 2 lands the helper + migration).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract loadFavoritesItems helper, fix keys, migrate legacy data</name>
  <files>src/features/favorites-storage.js, index.html</files>
  <behavior>
    - Helper reads new key (liked_items / watchlist_items)
    - Helper merges legacy items by id, writes consolidated list, removes legacy key
    - Helper returns [] safely when storage is empty/corrupt
  </behavior>
  <action>
    1. Create `src/features/favorites-storage.js` exporting `loadFavoritesItems(listType)` implementing the merge-and-migrate logic from RESEARCH.md.
    2. In `index.html`, update `loadFavoritesList(listType)` to call this helper (load via existing module/script pattern used by other features). Replace `item.poster` with `item.poster_path` and `item.type` with `item.media_type` in the render template. Per D-Track-D, keep the existing empty-state UI.
    3. Do NOT change writer code in detail.js — writers are already correct.
  </action>
  <verify>
    <automated>npx vitest run tests/favorites-storage.test.js</automated>
  </verify>
  <done>All Task 1 tests GREEN. Manual smoke (checkpoint task) confirms items appear in both tabs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Favorites + watchlist read from correct keys; legacy data migrated; item rendering uses correct fields.</what-built>
  <how-to-verify>
    1. Open the app, sign in, open any movie detail.
    2. Tap the like button → close detail → open Profile/Favorites → "Beğendiklerim" tab should show the item with its poster.
    3. Tap watchlist on another title → "İzlediklerim" tab should show it.
    4. (Migration) In DevTools Application tab, set localStorage["favorites"] to `[{"id":603,"media_type":"movie","poster_path":"/x.jpg","title":"Matrix"}]` and reload. Open Beğendiklerim → Matrix appears; the "favorites" key is gone; "liked_items" now contains Matrix.
    5. Empty case: clear all keys → both tabs show empty state, no errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/favorites-storage.test.js` passes
- `npm test -- --run` overall suite still green
- Manual smoke per checkpoint passes on mobile viewport
</verification>

<success_criteria>
- liked_items and watchlist_items are the single source of truth
- Legacy keys silently migrated and removed
- Render uses poster_path + media_type
- No regressions in existing detail tests
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-01-SUMMARY.md` after completion.
</output>
