---
phase: 03.2-polish-platform-gaps
plan: 02
subsystem: streaming
tags: [streaming, tmdb, providers, turkey, dedup]
requires:
  - TMDBService.getWatchProviders (existing)
  - RapidAPI Streaming Availability proxy (existing)
provides:
  - mergeWithTMDB(rapidApiProviders, tmdbId, type, countryUpper)
  - LOGO_OVERRIDES map + getLogoOverride()
  - PLATFORM_URLS entries: TV+, Turk Telekom TV+, Türk Telekom TV+
affects:
  - src/services/streaming-cache.js
  - src/lib/platforms.js
  - src/features/detail.js
tech-stack:
  added: []
  patterns: [two-layer-merge, lowercased-id-dedup, logo-cdn-override]
key-files:
  created:
    - tests/streaming-cache.test.js
  modified:
    - src/services/streaming-cache.js
    - src/lib/platforms.js
    - src/features/detail.js
decisions:
  - Merge runs only for countryUpper === 'TR' to avoid unnecessary TMDB calls
  - Validate ISO-2 country format before TMDB call (T-03.2-02-01 mitigation)
  - Dedup by lowercased serviceId AND serviceName (TMDB ids ≠ RapidAPI ids for same provider)
  - HBO Max + Max use TMDB CDN logo paths as overrides
metrics:
  completed: 2026-05-07
  tasks: 3
  status: implementation-complete-pending-manual-verify
---

# Phase 03.2 Plan 02: Streaming Provider Gaps Summary

Two-layer streaming merge (TMDB TR + RapidAPI) with serviceId dedup, missing platform URLs (TV+, Tabii), and HBO Max logo override.

## What Shipped

### Task 1 — RED tests (commit `3f7284e`)
6 tests in `tests/streaming-cache.test.js` covering merge, dedup, TMDB failure, null result, TMDB-only fallback, and ISO-2 validation.

### Task 2 — GREEN implementation (commit `1825a77`)
- `mergeWithTMDB()` exported from `src/services/streaming-cache.js`
- ISO-2 regex guard before TMDB call (T-03.2-02-01)
- Dedup by lowercased serviceId AND serviceName (catches TMDB ids ≠ RapidAPI ids)
- Failure-safe: TMDB throw or null returns RapidAPI list unchanged
- `_fetchFromApiOrFallback` runs the merge after every successful RapidAPI fetch when `countryUpper === 'TR'`

### Task 3 — Platform URLs + logo override + render dedup (commit `9936af0`)
- `src/lib/platforms.js`: added `TV+`, `Turk Telekom TV+`, `Türk Telekom TV+` URLs
- New `LOGO_OVERRIDES = { 'HBO Max': ..., 'Max': ... }` exported with `getLogoOverride()` helper
- `isTurkishPlatform()` recognizes TV+ variants
- `src/features/detail.js buildStreamingHTML`:
  - Defensive dedup pass on incoming providers (Set on lowercased serviceId/serviceName)
  - Logo override lookup before TMDB CDN fallback in tile rendering

## Test Status

`npx vitest run tests/streaming-cache.test.js` → 6/6 passed.

Pre-existing failures in `tests/api.test.js` (×2) and `tests/detail.test.js` (×4) are out of scope for this plan.

## Deviations from Plan

**None.** All three implementation tasks executed as specified.

The `Tabii` URL was already present in `PLATFORM_URLS` from a prior commit (`Phase 03.x`), so only `TV+` variants were appended.

## Checkpoint Pending

Task 4 is `checkpoint:human-verify (blocking)` — requires user manual verification:
1. TR country: open Turkish-original title → Gain/Exxen tile renders
2. Global title: Netflix appears once, HBO Max logo loads
3. TOD title: deep link works
4. TV+ / Tabii tiles open correct search URLs
5. US country: no TMDB merge calls in network tab

Resume signal: type "approved" or list providers still missing.

## Commits

- `3f7284e` test(03.2-02): add failing tests for mergeWithTMDB two-layer merge
- `1825a77` feat(03.2-02): implement mergeWithTMDB two-layer merge for TR streaming
- `9936af0` feat(03.2-02): add TV+/Tabii URLs, HBO Max logo override, dedup pass

## Self-Check: PASSED

- FOUND: tests/streaming-cache.test.js
- FOUND: src/services/streaming-cache.js (mergeWithTMDB exported)
- FOUND: src/lib/platforms.js (LOGO_OVERRIDES, TV+ URLs)
- FOUND: src/features/detail.js (getLogoOverride imported, dedup pass)
- FOUND commits: 3f7284e, 1825a77, 9936af0
- Tests: 6/6 GREEN
