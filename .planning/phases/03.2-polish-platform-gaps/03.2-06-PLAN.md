---
phase: 03.2-polish-platform-gaps
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/search-results.js
  - src/styles/
  - .claude/worktrees/
  - index.html
autonomous: true
requirements: [TRACK-F]
must_haves:
  truths:
    - "src/pages/search-results.js no longer exists; no import references it"
    - "Desktop-only CSS (sidebar, desktop layout, hover-only states) removed; mobile layout unaffected"
    - "Stale .claude/worktrees/ directories removed (4 obsolete agent worktrees)"
    - "All tests still pass after cleanup"
  artifacts:
    - path: "src/pages/search-results.js"
      provides: "DELETED — file must not exist"
    - path: "src/styles/"
      provides: "Cleaned of desktop sidebar / desktop-grid styles"
  key_links:
    - from: "build / runtime"
      to: "deleted modules"
      via: "no import statements remain"
      pattern: "search-results"
---

<objective>
Remove dead code, desktop CSS, and stale agent worktrees. Mobile-only project, so desktop patterns are pure noise.

Purpose: Track F. Reduce surface area before Phase 4. Per Pitfall 5, must verify no shared class names break mobile layout.
Output: Clean repo with verifiable no-regression test pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/pages/search-results.js + verify no references</name>
  <files>src/pages/search-results.js, index.html</files>
  <action>
    1. Grep the repo for `search-results` imports/script tags: `rg "search-results" --type js --type html`.
    2. Remove any `<script src="src/pages/search-results.js">` tag from `index.html`.
    3. Remove any imports of search-results from other JS files.
    4. Delete `src/pages/search-results.js`.
    5. Run full test suite to confirm nothing broke.
  </action>
  <verify>
    <automated>rg -q "src/pages/search-results" || echo "OK no refs"; npm test -- --run</automated>
  </verify>
  <done>File gone, no dangling refs, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove desktop-only CSS rules</name>
  <files>src/styles/</files>
  <action>
    1. Audit `src/styles/` for desktop patterns:
       - `@media (min-width: 1024px)` blocks for sidebar/desktop-grid
       - Selectors referencing `.sidebar`, `.desktop-nav`, `.desktop-only`
       - Hover-only states that are never reachable on touch (keep hover only when paired with focus/active fallbacks)
    2. For each candidate selector: search for matches in HTML/JS using `rg "<selector>" src/ index.html` BEFORE deletion (Pitfall 5).
    3. Delete only confirmed-unused rules. Leave any rule that mobile layout depends on.
    4. Run test suite + manual smoke at 375px viewport.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Desktop-only rules removed; mobile layout visually identical.</done>
</task>

<task type="auto">
  <name>Task 3: Clean .claude/worktrees/</name>
  <files>.claude/worktrees/</files>
  <action>
    1. List worktrees: `git worktree list`.
    2. For each entry under `.claude/worktrees/` that is no longer associated with an active branch/agent: `git worktree remove <path>` (or `--force` only if confirmed obsolete).
    3. Remove any leftover empty directories under `.claude/worktrees/`.
    4. Confirm `git status` is clean after removal.
  </action>
  <verify>
    <automated>git worktree list && git status --porcelain</automated>
  </verify>
  <done>Only active worktrees remain; no dangling directories.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes
- `rg "search-results"` returns no source matches
- `git worktree list` shows only active worktrees
- Manual mobile (375px) smoke: app renders identically to pre-cleanup baseline
</verification>

<success_criteria>
- Dead code removed without regression
- Desktop CSS gone but mobile pixel-identical
- Repo cleaner for Phase 4
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-06-SUMMARY.md` after completion.
</output>
