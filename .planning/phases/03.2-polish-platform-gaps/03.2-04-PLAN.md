---
phase: 03.2-polish-platform-gaps
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/detail.css
  - src/features/detail.js
autonomous: false
requirements: [TRACK-B]
must_haves:
  truths:
    - "User opening a movie now playing in cinemas sees a green ribbon badge with a soft pulse animation"
    - "User opening a movie with an upcoming theatrical release sees a red ribbon badge (no pulse)"
    - "Badge is anchored top-left of poster, visually distinctive, integrated with poster, never clipped by border-radius"
    - "Badge text remains in current i18n strings (no copy change)"
  artifacts:
    - path: "src/styles/detail.css"
      provides: "Redesigned .cinema-badge + .cinema-badge--active + @keyframes badge-pulse"
      contains: "badge-pulse"
  key_links:
    - from: "detail.js buildCinemaBadgeHTML"
      to: "detail.css .cinema-badge classes"
      via: "class names unchanged"
      pattern: "cinema-badge"
---

<objective>
Redesign the "Su an sinemalarda" / theatrical-release badge from a flat pill into an eye-catching ribbon with a subtle pulse on active.

Purpose: Track B locked decision. Current badge is easy to miss. RESEARCH.md provides the redesign spec (corner ribbon + gradient + box-shadow pulse).
Output: CSS-only redesign — no JS contract changes, no copy changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@src/styles/detail.css
@src/features/detail.js

<interfaces>
HTML emitted by buildCinemaBadgeHTML (unchanged):
  <div class="cinema-badge cinema-badge--active">…</div>  (active = now showing)
  <div class="cinema-badge">…</div>                       (upcoming)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign cinema badge CSS (ribbon + pulse)</name>
  <files>src/styles/detail.css</files>
  <action>
    Replace the existing `.cinema-badge` and `.cinema-badge--active` rules with the redesigned spec from RESEARCH.md "Cinema Badge Redesign":
    - Position absolute top:8px left:-2px, gradient red 135deg #E50914→#c20812 (upcoming/default), white text, 10px uppercase, letter-spacing 0.05em, padding 4px 10px 4px 8px, border-radius 0 4px 4px 0, red glow box-shadow, z-index 2.
    - `.cinema-badge--active`: green gradient #22c55e→#16a34a, green glow, animation `badge-pulse 2s ease-in-out infinite`.
    - Define `@keyframes badge-pulse` with 0/100% subtle glow → 50% intensified glow.
    - Ensure parent poster element has `overflow: visible` (not hidden) so the -2px left bleed is visible; if the current poster wrapper clips, add `.poster-wrap { overflow: visible }` scoped to the detail page only — verify no regression on movie cards elsewhere.
    - Respect `prefers-reduced-motion: reduce` → set animation: none on `.cinema-badge--active`.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>CSS compiles; existing detail tests still pass; no JS changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cinema badge ribbon redesign with conditional pulse.</what-built>
  <how-to-verify>
    1. Open a movie currently in theaters (recent release with `now_playing` true) → green ribbon at top-left, gentle pulse glow visible.
    2. Open an upcoming release → red ribbon, no animation.
    3. Verify ribbon is not clipped by poster border-radius; -2px left bleed shows.
    4. Toggle OS "Reduce Motion" → green badge stops pulsing but still green.
    5. Compare against an unrelated movie card grid → cards' rounded corners unaffected.
    6. Mobile viewport (375px) → ribbon legible, no overflow into safe area.
  </how-to-verify>
  <resume-signal>Type "approved" or share screenshots</resume-signal>
</task>

</tasks>

<verification>
- Test suite green (no JS changes)
- Visual inspection across at least one active + one upcoming title
</verification>

<success_criteria>
- CSS-only change scoped to detail page
- prefers-reduced-motion honored
- No regression in surrounding poster components
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-04-SUMMARY.md` after completion.
</output>
