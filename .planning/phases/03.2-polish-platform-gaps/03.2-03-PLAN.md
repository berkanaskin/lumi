---
phase: 03.2-polish-platform-gaps
plan: 03
type: execute
wave: 2
depends_on: [03.2-01]
files_modified:
  - public/services/auth.js
  - src/features/profile.js
  - src/lib/avatars.js
  - index.html
  - src/styles/profile.css
  - tests/profile-update.test.js
autonomous: false
requirements: [TRACK-E]
must_haves:
  truths:
    - "User can tap their display name in Profile and edit it inline; new name persists across reloads"
    - "User can tap their avatar and pick from a preset grid (10 avatars); selection persists across reloads"
    - "Changes propagate to Firebase Auth (updateProfile) AND Firestore users/{uid}"
    - "Profile stats show 'Beğenilen' (liked_items.length) and 'Listede Bekleyen' (watchlist_items.length) — meaningful counts"
    - "Google sign-in users still see their Google photo as default until they pick a preset"
  artifacts:
    - path: "public/services/auth.js"
      provides: "updateUserProfile(displayName, photoURL) method"
      contains: "updateUserProfile"
    - path: "src/lib/avatars.js"
      provides: "PRESET_AVATARS = [10 HTTPS URLs]"
    - path: "src/features/profile.js"
      provides: "openNameEditor, openAvatarPicker, refreshStats"
    - path: "tests/profile-update.test.js"
      provides: "Unit tests for updateUserProfile + stats decisioning"
  key_links:
    - from: "profile.js openNameEditor"
      to: "auth.js updateUserProfile"
      via: "await call with new displayName"
      pattern: "updateUserProfile"
    - from: "profile.js refreshStats"
      to: "localStorage liked_items / watchlist_items"
      via: "JSON.parse + .length"
      pattern: "liked_items|watchlist_items"
---

<objective>
Add profile customization: editable display name, preset avatar picker, meaningful stats. Persist to Firebase Auth + Firestore.

Purpose: Per Track E locked decisions, users must be able to set a custom nickname and avatar. Watch stats are reframed to use already-tracked liked/watchlist counts (Claude's discretion per CONTEXT.md).
Output: Working profile customization with one round-trip to Firebase per change, and stats that draw from the (now correct after Plan 01) localStorage keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@.planning/phases/03.2-polish-platform-gaps/03.2-01-SUMMARY.md
@public/services/auth.js
@src/features/profile.js
@index.html

<interfaces>
Existing (auth.js compat v8):
- this.auth = firebase.auth()
- this.db = firebase.firestore()
- syncUserToLocal(user) updates localStorage and dispatches authStateChanged

New:
- AuthService.updateUserProfile(displayName?: string, photoURL?: string): Promise<void>
- PRESET_AVATARS: string[] (10 HTTPS CDN URLs, movie-themed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — profile update unit test</name>
  <files>tests/profile-update.test.js</files>
  <behavior>
    - Test 1: updateUserProfile(name) calls user.updateProfile({displayName: name}) and db.users.doc(uid).update({displayName: name}).
    - Test 2: updateUserProfile(undefined, photoURL) only sets photoURL.
    - Test 3: When currentUser is null → throws 'Not logged in'.
    - Test 4: After successful update, syncUserToLocal is invoked with refreshed user.
  </behavior>
  <action>
    Create `tests/profile-update.test.js`. Mock firebase.auth() and firestore() per existing patterns in tests/auth.test.js (or build minimal mocks if no precedent). Import AuthService instance.
  </action>
  <verify>
    <automated>npx vitest run tests/profile-update.test.js</automated>
  </verify>
  <done>4 tests RED until Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AuthService.updateUserProfile + preset avatar list</name>
  <files>public/services/auth.js, src/lib/avatars.js</files>
  <behavior>
    - Updates Firebase Auth + Firestore atomically (best-effort: Auth first, then Firestore; one error logs but does not roll back the other).
    - Sets only the fields provided.
    - Calls syncUserToLocal(this.auth.currentUser) on success.
  </behavior>
  <action>
    1. In `public/services/auth.js`, add `updateUserProfile(displayName, photoURL)` exactly per RESEARCH.md "Firebase Profile Update (compat v8)" snippet, plus the syncUserToLocal call from Pitfall 3.
    2. Create `src/lib/avatars.js` exporting `PRESET_AVATARS` — 10 movie-themed HTTPS URLs. Use TMDB image CDN paths or freely-licensed icon URLs. Each entry: `{ id: 'avatar-01', url: 'https://...' , label: '...' }`. Per Pitfall 4: HTTPS only.
  </action>
  <verify>
    <automated>npx vitest run tests/profile-update.test.js</automated>
  </verify>
  <done>All Task 1 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire profile UI — name editor, avatar picker, stats refresh</name>
  <files>src/features/profile.js, index.html, src/styles/profile.css</files>
  <action>
    1. `index.html` profile section: add a small pencil icon overlay on `#profile-avatar-container`; make `#profile-name` tap-to-edit (swap to `<input>` on tap, blur/Enter commits). Add a hidden `#avatar-picker-modal` containing a 5x2 grid of preset avatars + close button.
    2. `src/features/profile.js`:
       - openNameEditor(): replace name node with input, on commit call `AuthService.updateUserProfile(newName)`, optimistic UI, revert + toast on error.
       - openAvatarPicker(): render PRESET_AVATARS into the modal grid; tapping a tile calls `updateUserProfile(undefined, url)` and closes modal.
       - refreshStats(): read `liked_items` and `watchlist_items` (post Plan 01); render labels "Beğenilen" and "Listede Bekleyen". Update DOM ids accordingly (rename existing `stat-movies` label, replace `stat-hours` content).
       - On `authStateChanged` event, refresh avatar img + name + stats.
    3. `src/styles/profile.css`: minimal additions for picker modal (mobile-only — no desktop variants per project rule), pencil overlay, and active-tile state. Reuse design tokens (`--bg-elevated`, `--radius-full`, etc.).
  </action>
  <verify>
    <automated>npx vitest run tests/profile-update.test.js && npm test -- --run</automated>
  </verify>
  <done>Profile UI shows pencil icon + tap-to-edit name + avatar picker; stats use new labels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profile customization (name + avatar) wired to Firebase + Firestore + meaningful stats.</what-built>
  <how-to-verify>
    1. Sign in with Google → profile shows Google photo + name as defaults.
    2. Tap name → input appears → type "Test123" → Enter → toast "Updated"; reload → name still "Test123".
    3. Tap pencil over avatar → modal opens with 10 tiles → pick one → modal closes → avatar updates immediately; reload → preset persists.
    4. Verify Firestore document `users/{uid}` has `displayName: "Test123"` and `photoURL: "<preset url>"`.
    5. Sign out and sign in with email account → repeat 2-3.
    6. Stats: "Beğenilen" reflects count of liked items; "Listede Bekleyen" reflects watchlist count. Add/remove a like → returning to profile shows updated count.
    7. Offline: turn off network, attempt name edit → see error toast, name reverts.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Firebase Auth | Authenticated user updates own profile only |
| client → Firestore | Security rules must restrict users/{uid} writes to that uid |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-03-01 | Tampering | photoURL field | mitigate | Validate URL is HTTPS and from allowlisted hosts (TMDB CDN, googleusercontent) before sending |
| T-03.2-03-02 | XSS via displayName | profile DOM render | mitigate | Use textContent (never innerHTML) when rendering name; trim + length cap 40 chars |
| T-03.2-03-03 | Authz | Firestore users/{uid} write | mitigate | Confirm firestore.rules already restricts users/{uid} write to request.auth.uid == uid (verify in rule file) |
| T-03.2-03-04 | Information Disclosure | photoURL stored | accept | Public-readable URL; no PII beyond what user chose |
</threat_model>

<verification>
- Vitest profile-update suite passes
- Manual flow per checkpoint passes
- firestore.rules confirmed (no change needed if already correct)
</verification>

<success_criteria>
- updateUserProfile is a single-method API used by both name + avatar flows
- Preset avatars load from HTTPS CDN
- Stats labels updated; counts reflect localStorage truth
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-03-SUMMARY.md` after completion.
</output>
