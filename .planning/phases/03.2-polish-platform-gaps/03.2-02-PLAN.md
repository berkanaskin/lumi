---
phase: 03.2-polish-platform-gaps
plan: 02
type: execute
wave: 2
depends_on: [03.2-01]
files_modified:
  - src/services/streaming-cache.js
  - src/lib/platforms.js
  - src/features/detail.js
  - tests/streaming-cache.test.js
autonomous: false
requirements: [TRACK-A]
must_haves:
  truths:
    - "For Turkish content, user sees Gain/Exxen/TOD/TV+/BluTV/Tabii when those providers cover the title (via TMDB TR data)"
    - "Existing Netflix/Disney+/Amazon Prime results from RapidAPI still render"
    - "No duplicate provider tiles appear when both APIs return the same provider"
    - "HBO Max logo renders (local logo lookup overrides broken CDN URL)"
    - "Tapping a Turkish provider tile opens its deep link (TV+, Tabii URLs added)"
  artifacts:
    - path: "src/services/streaming-cache.js"
      provides: "mergeWithTMDB() merges TMDB TR providers into RapidAPI result"
      contains: "mergeWithTMDB"
    - path: "src/lib/platforms.js"
      provides: "TV+ and Tabii URL entries; logo overrides for HBO Max"
      contains: "tvplus.com.tr"
    - path: "tests/streaming-cache.test.js"
      provides: "Unit tests for merge + dedup + fallback behavior"
  key_links:
    - from: "streaming-cache.js _fetchFromApiOrFallback"
      to: "TMDBService.getWatchProviders"
      via: "always-on TR merge after RapidAPI success"
      pattern: "mergeWithTMDB|getWatchProviders"
    - from: "detail.js buildStreamingHTML"
      to: "platforms.js logoOverrides"
      via: "logo URL lookup before falling back to <img onerror>"
      pattern: "logoOverrides|HBO Max"
---

<objective>
Make Turkish streaming providers (Gain, Exxen, TOD, TV+, Tabii, BluTV) appear on detail pages by merging TMDB TR watch-providers into the RapidAPI Streaming Availability result.

Purpose: RapidAPI's Streaming Availability dataset only covers BluTV for Turkey. TMDB watch providers TR has the rest. RESEARCH.md confirms a two-layer merge with serviceId dedup is the correct fix.
Output: Two-layer provider source, deduplication, missing platform URLs (TV+, Tabii), HBO Max logo override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-polish-platform-gaps/03.2-CONTEXT.md
@.planning/phases/03.2-polish-platform-gaps/03.2-RESEARCH.md
@src/services/streaming-cache.js
@src/lib/platforms.js
@src/features/detail.js

<interfaces>
Existing (do not change shape):
- streaming-cache.js exports `getStreamingAvailability(tmdbId, type, country)` returning `{ providers: [...], updatedAt }`
- Provider shape (normalized): `{ serviceId, serviceName, type, group, link, logoPath, themeColor, quality }`
- TMDBService.getWatchProviders(tmdbId, type, country) returns `{ flatrate?, free?, rent?, buy?, link? }` with provider shape `{ provider_id, provider_name, logo_path, display_priority }`

New (this plan):
- mergeWithTMDB(rapidApiProviders, tmdbId, type, countryUpper) → Promise<Provider[]>
- platforms.js: PLATFORM_URLS adds 'TV+', 'Tabii'; new export LOGO_OVERRIDES = { 'HBO Max': '<known good URL>' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — streaming-cache merge tests</name>
  <files>tests/streaming-cache.test.js</files>
  <behavior>
    - Test 1: mergeWithTMDB appends TMDB providers absent from RapidAPI result (Gain not in RapidAPI → appears in merged output).
    - Test 2: Dedup by serviceId (case-insensitive) — Netflix from both sources appears once, RapidAPI version preferred.
    - Test 3: When TMDBService.getWatchProviders throws/returns null, original RapidAPI providers returned unchanged.
    - Test 4: When RapidAPI list is empty (TMDB-only fallback), TMDB providers normalized correctly with serviceId/serviceName/group.
  </behavior>
  <action>
    Create `tests/streaming-cache.test.js`. Mock TMDBService via vi.mock. Import `mergeWithTMDB` (will be exported in Task 2). Construct fixtures using the shapes documented in RESEARCH.md "Streaming Two-Layer Merge Pattern".
  </action>
  <verify>
    <automated>npx vitest run tests/streaming-cache.test.js</automated>
  </verify>
  <done>4 tests defined and RED until Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mergeWithTMDB + always-on TR merge in streaming-cache.js</name>
  <files>src/services/streaming-cache.js</files>
  <behavior>
    - mergeWithTMDB normalizes TMDB providers and dedups against RapidAPI list by lowercase serviceId.
    - _fetchFromApiOrFallback runs the merge AFTER any successful RapidAPI fetch when country === 'TR' (per RESEARCH.md — to avoid extra TMDB calls in non-TR markets we only merge for TR; do not gate on success otherwise).
    - Existing _tmdbFallback path on RapidAPI failure remains unchanged.
    - Cache key/TTL behavior unchanged (merged result is cached as the canonical providers array).
  </behavior>
  <action>
    1. Implement `mergeWithTMDB(rapidApiProviders, tmdbId, type, countryUpper)` exactly per RESEARCH.md "Streaming Two-Layer Merge Pattern" snippet. Reuse the GROUP_MAP already in the file; if absent, define `{ subscription: 'stream', free: 'free', rent: 'rent', buy: 'buy' }`.
    2. In `_fetchFromApiOrFallback` (or equivalent orchestrator), call mergeWithTMDB on the RapidAPI providers when `countryUpper === 'TR'` before persisting to cache.
    3. Export mergeWithTMDB for testing (named export).
    4. No new dependencies; no schema migrations to Firestore cache (merged providers fit existing shape).
  </action>
  <verify>
    <automated>npx vitest run tests/streaming-cache.test.js</automated>
  </verify>
  <done>All Task 1 tests GREEN. Existing streaming tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add missing platform URLs + HBO Max logo override + dedup in render</name>
  <files>src/lib/platforms.js, src/features/detail.js</files>
  <action>
    1. In `src/lib/platforms.js`:
       - Add to PLATFORM_URLS (or equivalent map):
         - `'TV+': 'https://www.tvplus.com.tr/search?q='`
         - `'Turk Telekom TV+': 'https://www.tvplus.com.tr/search?q='` (TMDB variant)
         - `'Tabii': 'https://www.tabii.com/tr/search?q='`
       - Export `LOGO_OVERRIDES` map with at least `{ 'HBO Max': '<https://image.tmdb.org/t/p/original/aS2zvJWn9mwiCOeaaCkIh4wleZS.png>' }` (HBO Max TMDB logo path; verify file exists).
    2. In `src/features/detail.js buildStreamingHTML`:
       - Before rendering each provider tile, look up `LOGO_OVERRIDES[provider.serviceName]` and use it instead of `provider.logoPath` when present.
       - Apply final dedup pass per RESEARCH.md "Duplicate Results Fix" snippet (Set on lowercased serviceId/serviceName) — defensive double-check after merge.
  </action>
  <verify>
    <automated>npx vitest run tests/streaming-cache.test.js tests/detail.test.js</automated>
  </verify>
  <done>HBO Max tile shows a logo; TV+/Tabii tiles open correct deep links; no duplicate tiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two-layer streaming merge for Turkey + missing platform URLs + HBO Max logo fix.</what-built>
  <how-to-verify>
    1. Set country to TR in app (or via country selector).
    2. Open a known Turkish-original (e.g., Gain or Exxen show) → Where to Watch should list Gain/Exxen/etc.
    3. Open a global title (e.g., Stranger Things) → Netflix appears once (no duplicate), HBO Max logo loads when present.
    4. Open a beIN/TOD title and verify TOD tile renders with a working deep link.
    5. Tap TV+ and Tabii tiles when available → opens their search URL in new tab.
    6. Switch country to US → only RapidAPI providers, no TMDB merge calls (verify in network tab).
  </how-to-verify>
  <resume-signal>Type "approved" or list providers still missing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → /api/streaming-availability | RapidAPI key never leaves Edge Function |
| client → TMDB | Public read, country param is user-controlled but bounded to ISO codes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-02-01 | Tampering | country param to TMDB call | mitigate | Validate countryUpper matches /^[A-Z]{2}$/ before calling TMDB |
| T-03.2-02-02 | Information Disclosure | Cached providers | accept | No PII; public availability data |
| T-03.2-02-03 | Denial of Service | Extra TMDB call per detail load (TR only) | mitigate | Result cached in existing Firestore streaming cache; same TTL as RapidAPI result |
</threat_model>

<verification>
- All vitest suites pass
- Manual TR smoke shows Turkish providers
- No duplicates across multiple titles
</verification>

<success_criteria>
- TMDB TR merge runs only for TR country
- Dedup by serviceId is case-insensitive
- HBO Max + TV+ + Tabii render correctly
- Failure modes degrade gracefully (TMDB error → original RapidAPI list)
</success_criteria>

<output>
Create `.planning/phases/03.2-polish-platform-gaps/03.2-02-SUMMARY.md` after completion.
</output>
